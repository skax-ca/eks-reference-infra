# Notepad — iac-reference-infra

## Priority Context
eks-reference-infra — hub-spoke EKS GitOps 레퍼런스 배포 루트. 2026-08-26 09:44~11:00 KST경(별도 세션, session-end 미실행): hub 철거→재구축 완주, GitOps 시딩까지 완료(2026-08-27 사후 검증: Application 10/10 Healthy, 노드 2/2 Ready). 현재 hub 정상 가동 중·dev(spoke) 미배포. 다음: 사용자가 hub 철거 시점 결정 예정(미확정).

## Working Memory
### 2026-08-24 07:59
2026-08-24 — CLAUDE.md·README.md(9개)·AGENTS.md(13개)·docs/deployment-facts.md 삭제(사용자 지시, "현행화하기만 어렵다"는 이유 — 필요시 재정의/deepinit으로 재생성 예정). module repo(iac-module-library) docs/06-conventions.md에 §9 문체 규칙을 신규 작성해 SSOT 실체를 채움(이전엔 이 repo CLAUDE.md가 「9. 문체 규칙」을 참조만 하고 실체가 없던 drift). em-dash 규칙: grandfather 폐지, 즉시 전면 정리로 전환. 남긴 3개 SSOT 문서(hub-lifecycle·spoke-lifecycle·runbooks)는 stop-slop 8개 규칙 전체로 재검토 완료(실질 위반은 em-dash뿐, 나머지는 이미 충족) — 검증 스크립트 통과. em-dash 일괄정리용 서브에이전트 6개를 병렬로 띄웠으나 전부 계정 단위 세션 한도(리셋 6:50pm KST)에 걸려 실패, 부분 진행분만 남아 직접 이어서 완료했음(subagent 위임은 실패 시 진행 상황이 uncommitted 상태로만 남고 알림도 사후에 옴 — 대량 문서 편집엔 신뢰도 낮음, 기록할 가치). module repo 쪽 남은 em-dash 정리는 사용자가 별도 세션에서 진행하기로 함(이 세션 스코프 아님).
### 2026-08-25 00:34
2026-08-25 — docs/hub-lifecycle.md·spoke-lifecycle.md·runbooks.md를 "Claude Code가 그대로 실행해도 되는가"·"팀 공유 가독성" 관점으로 실측 리뷰(코드·워크플로·git 이력 대조). 발견·수정: (1) hub-lifecycle.md 구축 절차에 TGW apply 단계가 통째로 누락 — deploy-hub-tgw.yml을 deploy-hub-network.yml보다 먼저 돌려야 하는데 문서엔 없어 재현 가능한 Invalid for_each argument 에러로 이어짐, §4를 "TGW와 네트워크(L1)"로 확장해 해결. (2) bootstrap/README.md가 8/24 문서 정리로 삭제됐는데 hub/spoke-lifecycle.md가 SSOT로 계속 링크 — bootstrap/config.sh로 교체(단, config.sh 자신도 deployment-facts.md §2를 가리키는 죽은 참조가 남아있음, 미해결). (3) spoke-lifecycle.md의 deployment-facts.md 죽은 참조("hub가 destroy된 경우" TGW 프리픽스리스트 재발급 절차)는 git 이력(c8db4b2~1)에서 원문을 찾아 인라인 복원. (4) runbooks.md §6이 존재하지 않는 deploy-eks.yml을 참조 — deploy-hub-eks.yml/deploy-dev-eks.yml로 정정. (5) 세 문서 제목의 03./04./07. 잔재 번호 제거(사용자 지적, module repo 연속 문서 체계 잔재였음). 전부 scripts/validate-doc-conventions.py 통과, 커밋 72d0147.

이어서 CLAUDE.md 재정의: module repo(iac-module-library) CLAUDE.md(262줄)를 그대로 상속하지 않기로 결정 — 근거 (a) Claude Code 공식 문서 확인 결과 working directory 밖 @import는 팀원마다 승인 대화상자 뜨고 로컬 클론이 정확히 형제 디렉토리여야 동작해 팀 공유 파일에 부적합, (b) module CLAUDE.md 자체가 이미 공식 권장 200줄을 넘고 모듈 저작 전용 내용(facade·모듈별 semver·tofu test 게이트)이 태반이라 소비 repo와 무관. 대신 이 repo 사실 기준으로 108줄 새로 작성(repo 위치/SSOT 경계·저장소 구조·실행모델·네이밍·로컬게이트·브랜치PR규칙·문서규칙·모듈인자확인). 작업 중 module repo가 docs/06-conventions.md를 docs/conventions.md로 이미 리네임했음을 발견 — 이 repo의 scripts/validate-doc-conventions.py·.githooks/pre-commit·notepad-sync SKILL.md 3곳의 죽은 경로 참조를 같이 정정. 커밋 a77810c, 둘 다 push 완료(12c8fdc..a77810c).

미해결로 남긴 것: ① bootstrap/README.md 죽은 링크가 .github/workflows/deploy-hub-eks.yml:17·deploy-hub-network.yml:32 주석에도 남아있음(후자는 deployment-facts.md §3도 같이 참조) — 오늘 범위(docs/*.md 3개) 밖이라 손대지 않음. ② TGW 철거(destroy) 절차가 hub-lifecycle.md 어디에도 없음(deploy-hub-tgw.yml은 destroy도 지원하는데 문서화 안 됨) — 정확한 순서(네트워크 destroy 먼저, TGW 나중)에 대한 실측 근거가 없어 추측으로 채우지 않고 보류.
### 2026-08-25 01:43
2026-08-25 (이어서) — 사용자 요청으로 docs 외 전체 코드(.sh·.tf·워크플로 .yml)의 주석 품질을 모범사례 기준으로 검토·개선. 리서치 결과 외부 모범사례를 새로 찾을 필요 없이 iac-module-library docs/conventions.md §5(코드 규약→주석)에 이미 구체적 기준이 있었음을 발견: 주석은 "왜 이 값인가"·"바꾸면 무엇이 깨지는가" 둘 중 하나에만 답해야 하고, 날짜·결정 식별자(D-...)·문서 절 번호·"실측했다" 류 사건 서술을 본문에 쓰지 않는다(git blame·커밋 메시지의 몫). 이 기준으로 이 repo의 .tf/.sh/워크플로 yml을 grep 실측 스캔한 결과 주석 라인 기준 122건(42개 파일 중 15개, 워크플로 72·부트스트랩 20·live/**/*.tf 30) 위반을 확인. 사용자가 "기존 위반 정리부터"를 선택해 파일별로 직접 재작성(정보 손실 없이 날짜/D번호/§인용만 제거, "왜/무엇이 깨지는가"는 보존) → 5개 워크플로 YAML(deploy-hub/dev-network·eks·tgw) → bootstrap/{config,bootstrap,verify}.sh + scripts/argocd-seed.sh → live/{hub,dev}/{networking,eks}/main.tf + live/hub/tgw/main.tf + live/hub/networking/outputs.tf 순서로 전부 정리. 커밋 f03275c, push 완료(f2cff8f..f03275c). 과정에서 이전 세션에 남겨둔 open-item(bootstrap/README.md·docs/deployment-facts.md 죽은 참조 2곳)이 자연스럽게 함께 해소됐고, 추가로 live/hub/tgw/README.md(애초에 존재한 적 없는 파일)를 가리키던 죽은 인용 2곳도 새로 발견해 제거했다. 5개 워크플로 YAML 파싱·5개 스크립트 bash 문법·5개 tf 루트 tofu fmt/validate 전부 재검증 통과, pre-commit(tflint·trivy)·pre-push(tofu validate) 훅도 통과.

남긴 것: 이 주석 규칙(§5)에 대한 자동 검증 스크립트가 아직 없다 — scripts/validate-doc-conventions.py 같은 pre-commit 게이트가 없어 앞으로 같은 패턴(날짜·D번호 재유입)이 재발할 수 있음. module repo 자신도 17건 잔존(완전 강제된 규칙 아님) 확인됨. 다음에 여유 있으면 .tf/.sh/.yml 주석 라인에서 날짜·D번호 패턴을 잡는 pre-commit 훅 신설을 제안했었음(이번 세션엔 미착수, 기존 위반 정리만 완료).
### 2026-08-25 04:44
2026-08-25 (세션 이어서) — hub 재구축 검증 세션. docs/hub-lifecycle.md 4~7절대로 hub TGW(신규 분리 root 첫 apply)→networking→EKS+workbench→GitOps seed 순서로 처음부터 완주. 도중 발견·수정한 실제 버그: ①live/hub/networking의 aws_route.vpc_to_spoke for_each가 toset()/setproduct()에 fresh VPC의 unknown 라우트테이블 ID를 섞어 Invalid for_each argument — 브랜치→PR#38(OIDC가 main ref만 신뢰해 브랜치에서 사전 테스트 불가, merge 후 main에서 재시도해 확인) 후 merge(4f926e8)로 해결. ②GitOps seed 중 root-app이 "authentication required: Repository not found" — eks-platform-gitops(별도 repo) 내 bootstrap/root-app.yaml 등 9개 파일이 08-24 repo rename 이후에도 옛 이름(iac-platform-gitops/iac-reference-infra) 참조 중이었음, repo 경계 원칙을 사용자 승인하에 넘어 그 repo에서 직접 수정·커밋(ff44ddb). GitHub App private key 취급을 두고 사용자가 "팀 전용 계정이니 상시 저장하자"고 제안했으나, 실측(EC2 인스턴스 목록에 무관한 개인 리소스 다수 확인)으로 계정이 예상보다 훨씬 넓게 공유됨을 보여 자동화(업로드 직전·삭제 직후, 상시 저장은 안 함) 쪽으로 사용자가 재확정. hub 완전 가동 확인(Application 10/10 Synced/Healthy) 후 ArgoCD 콘솔 터널 열어 admin 초기 비밀번호 확인(비밀번호 교체는 곧 철거할 거라 사용자가 명시적으로 건너뜀). 이어서 hub 철거(8~13절) 실행 — EKS destroy→networking destroy→(신규, 문서에 없던) TGW destroy 순서로 시도해 attachment 관련 에러 없이 전부 성공, teardown-verify.sh exit 0. 이 TGW destroy 순서를 docs/hub-lifecycle.md 12절에 반영(400줄 제한 맞추려 14절 "노드만 줄이기"·"야간 정지" 중복 병합), commit b1153cd. 세션 끝: hub·dev 둘 다 미배포 상태로 복귀(리허설 성격), 다음 착수 후보는 project memory 참조.
### 2026-08-25 05:26
2026-08-25 (세션 이어서) — 8/24 일괄 삭제된 README.md 8개 중 실제로 코드·문서가 참조하는 곳을 grep으로 실측 조사. bootstrap/verify.sh·config.sh, docs/hub-lifecycle.md·spoke-lifecycle.md는 bootstrap/README.md를, live/{hub,dev}/{networking,eks}/backend.tf·variables.tf 6곳은 각 디렉토리 README.md를 가리키는 죽은 링크였음이 확인됐다. 그런데 live/*/backend.tf·variables.tf 원문을 직접 읽어보니 코드 주석 자체가 이미 완결적 설명(①②③ 근거·주입경로·경고문 전부 인라인)이라 README.md에 실제로 위임된 내용이 없었고, 과거 이 파일들이 모듈 버전 stale(eks-cluster-v0.4.0 등)로 문제됐던 전례(project-memory gotcha)까지 감안해 4개는 복원하지 않기로 사용자와 합의했다 — 유지보수 비용 최소화가 목적. 반면 bootstrap/config.sh:4-5가 "README.md 표가 코드의 SSOT다, 어긋나면 README를 고친다"고 코드 자체가 명시한 bootstrap/README.md와, GitHub App private key SSM 전달 절차·기각 대안(직접 붙여넣기·send-command·Secrets Manager·ESO)을 담은 scripts/README.md 2개는 코드가 대체할 수 없는 판단 근거를 담고 있어 복원 대상으로 확정했다.

WebSearch로 runbook 작성 모범사례(Docsie·AWS Well-Architected: 목적 명시·원하는 결과·필요 도구/권한·신규 팀원이 그대로 따라할 수 있는지가 테스트 기준)를 확인한 뒤, 삭제 전 원문(bootstrap 227줄·scripts 277줄)에서 D24·D27-1·"23 §5" 같은 결정 식별자·문서 절 인용을 전부 걷어내고(이번 세션 초반 코드 주석 정리 때 적용한 것과 같은 원칙) config.sh·bootstrap.sh·verify.sh·argocd-seed.sh·teardown-verify.sh 실물과 대조해 최신 상태로 재작성했다(bootstrap/README.md 207줄, scripts/README.md 220줄). bootstrap/verify.sh·config.sh, docs/hub-lifecycle.md·spoke-lifecycle.md의 죽은 참조 4곳도 함께 정정했다.

validate-doc-conventions.py 통과 확인 후 사용자 요청으로 재검토를 한 번 더 수행해 실제 결함 3건을 추가로 발견·수정했다: ① bootstrap/README.md 상단 요약표가 「4. 검증」·「5. IaC 승격 경로」를 가리켰는데 실제 절 번호는 「3.」·「4.」였음(절 병합 후 상단 표 갱신을 놓쳤다), ② scripts/README.md의 「자기소멸 원칙」 인용이 실제 헤딩("설계 제약: 자기소멸(self-superseding)")과 글자 그대로 안 맞았음(헤딩을 "자기소멸 원칙 (self-superseding)"으로 정정), ③ sub 패턴 예시에서 이미 config.sh에 평문 커밋된 GH_ORG_ID·GH_REPO_ID 값(310520211/1316830050, AWS 계정 ID와 달리 비민감정보)을 불필요하게 &lt;org id&gt; 플레이스홀더로 가려 오히려 덜 유용했던 것을 실값으로 복원. 재검증 통과. 커밋은 세션 종료 절차에서 진행 예정.
### 2026-08-25 07:08
2026-08-25 (이어서, hub 재배포 세션) — iac-module-library가 Azure 지원 추가로 modules/ 디렉토리 구조 재편 중(옆 세션 진행)이라는 사용자 보고를 받고, 이 repo의 module source가 git 태그(`ref=vpc-v0.3.0` 등)로 pin돼 있어 과거 태그가 가리키는 커밋 트리는 재편 이후에도 그대로 유효함을 근거로 재배포 진행에 지장 없다고 판단·설명. 사용자가 애초엔 Azure 반영을 기다리기로 했으나 "시간이 좀 더 걸릴거 같다"며 hub 재배포를 바로 진행하기로 변경.

docs/hub-lifecycle.md 4~6절대로 TGW apply(2m28s)→networking apply(약 4.5분)→EKS+workbench apply(약 14분, 노드 2/2 Ready)까지 워크플로 전부 success. GitOps seed(6절) 진행 중 사용자가 "절차 바꼈자나"라며 제동 — 실제로는 절차가 바뀐 게 아니라 원래부터 문서화되지 않은 두 단계가 있었음을 재확인하는 과정이었다: ① GitHub App(pem) 자체를 만드는 절차(App 생성·설치·key 발급)가 이 repo 어디에도 없음(scripts/README.md는 pem이 이미 로컬에 있다는 전제로 시작), ② GITOPS_REPO_URL은 ArgoCD 자신의 repository Secret용일 뿐, GITOPS_REPO_DIR(workbench 로컬 clone본)을 private repo에서 어떻게 가져오는지는 스크립트도 문서도 다루지 않음(workbench에 gh CLI도 git credential도 없음, 실측 확인). 사용자 승인 하에 이미 있는 gitops-reader App(App ID 4512318, Installation ID 151838919)의 pem으로 GitHub App installation access token을 workbench의 curl+openssl+jq로 직접 발급해 `git clone https://x-access-token:<token>@...`로 clone하는 방식을 새로 만들어 진행. pem은 SSM SecureString으로 노트북→workbench 전달(scripts/README.md 기존 절차 그대로) 후 즉시 shred+SSM 파라미터 삭제 완료.

argocd-seed.sh --dry-run 통과 후 실제 실행, 5단계(helm install→GitHub App Secret→AppProject→cluster Secret→root Application) 전부 성공. root-app이 실제 커밋 SHA(ff44ddb) 기준 Synced/Healthy로 흡수됐고, 이어서 5분 이내 하위 Application 9개(aws-lbc·cluster-autoscaler·karpenter·karpenter-nodepool·keda·kyverno 3종)까지 전부 Synced/Healthy로 수렴 완료(cascade sync 자연 수렴, 수동 개입 없음). ArgoCD 관리자 비밀번호 교체(완료 조건 마지막 항목)는 사용자가 "이번에도 teardown 예정"이라며 명시적으로 스킵 — 지난 세션과 동일한 패턴(곧 철거할 인프라에 비밀번호 교체는 낭비).

두 문서 공백은 project memory open-items에 기록 완료, scripts/README.md에 새 하위 절로 정식 문서화하는 건 아직 미착수(성공한 절차를 검증했으니 다음 여유 있을 때 반영 권장). 세션 종료 시점 상태: hub는 완전히 살아있고 정상 가동 중(10/10 Synced/Healthy), dev(spoke)는 미배포. 다음 세션 또는 이 세션 뒷부분에 사용자가 철거 여부·시점을 결정할 예정 — 아직 미확정.
### 2026-08-26 00:06
2026-08-26 — 지난 세션(2026-08-25 hub 재배포) open-item으로 남긴 문서 공백 2건을 리서치·반영. GitHub 공식 문서(docs.github.com) 5개 페이지를 WebFetch로 확인: App 등록 절차, private key 발급/보관 권장사항, installation access token 발급 API, JWT(RS256) 생성 시 클레임 규칙(iat=현재-60초, exp=최대10분, iss=client ID), Only select repositories로 설치 범위 제한하는 법. Installation ID 확인법은 공식 문서에 웹 UI 경로가 명시되어 있지 않아 API(GET /orgs/{org}/installation) 방식과 실전 대안(Configure 버튼 URL의 마지막 세그먼트)을 함께 제시.

scripts/README.md에 두 절 신설(220→284줄): "GitHub App 만들기"(등록→Contents:Read-only 권한→PEM 발급→설치→Installation ID 확인 5단계), "GITOPS_REPO_DIR: private 저장소를 workbench로 clone하기"(curl+openssl+jq로 JWT 서명→installation access token 교환→clone→remote URL에서 토큰 제거까지). 마지막 단계(clone 직후 remote set-url로 토큰 제거)는 지난 세션 절차에는 없던 것을 이번에 추가 제안 — 이 repo의 기존 위생 원칙(pem 상시 보관 안 함, 필요할 때만 SSM으로 전달 후 즉시 삭제)과 일관되게 토큰도 .git/config에 남기지 않도록.

hub-lifecycle.md §0 준비물 표의 "GitHub App이 필요하다" 항목에 새 절 링크 추가(398줄 유지, 표 셀 안에 링크만 추가해 줄 수 변동 없음). validate-doc-conventions.py 통과. 반영 위치(scripts/README.md vs hub-lifecycle.md 인라인)는 AskUserQuestion으로 확인 후 진행, 커밋 시점도 AskUserQuestion으로 확인 후 즉시 커밋(사용자가 "지금 커밋" 선택). commit d6b01c7, push 완료. 이 세션엔 인프라 액션(hub 철거 등) 없음 — 문서 작업만 진행.
### 2026-08-27 00:29
2026-08-27 — 사용자가 "어제 설치 진행 중 갑자기 down됐다"고 보고, 진상 확인 세션. project memory엔 2026-08-26 세션이 "문서 작업만, 인프라 액션 없음"으로 기록돼 있어 실제 상태와 대조 필요했음. GitHub Actions 이력(gh run list)과 workflow 로그(plan 요약의 add/destroy 카운트)를 대조해 2026-08-26 09:44~11:00 KST경 별도 세션에서 hub 철거(eks 87개→networking 69개→tgw 7개 destroy)→재구축(tgw→networking→eks 순 apply) 사이클이 전부 성공했음을 확인. 이어서 SSM으로 workbench(i-013944f973fcfaacd)에 직접 접속해 kubectl로 실물 상태 조회 — 노드 2/2 Ready, ArgoCD Application 10/10 Synced/Healthy, GitOps 시딩까지 완료된 상태임을 검증. dev/eks push-plan 실패("no matching EC2 VPC found")는 dev networking 미배포 상태에서 나는 예상된 순서 문제일 뿐 버그 아님(dev는 여전히 미배포, 변동 없음).

사용자 확인: "다운"은 세션/노트북이 꺼진 것이었고, 인프라 설치 자체는 이미 끝나 있었음(session-end 미실행으로 notepad에 기록만 안 남음). 결론: 재작업 불필요, hub 정상 가동 확인 완료. 코드 변경 없음, 조사만 수행한 세션.
### 2026-09-03 (세션 시작·메모리 위생 세션)
2026-09-03 — 인프라 액션 없음, notepad/project-memory 위생 점검 세션. (1) notepad.md Working Memory 섹션이 과거 세션들에서 append 대신 전체 재붙여넣기로 5~6회 그대로 중복돼 있었음을 세션 시작 직후 발견(1372줄→1240줄, 132줄 순삭제, `git diff` 결과 삭제만 있음을 확인 후 반영). MANUAL 아카이브는 자동 로드되지 않는 영구 기록이라 이번엔 손대지 않음(거기도 2026-08-06 구간이 한 번 중복돼 있으나 세션마다 컨텍스트를 잡아먹지 않아 후순위로 보류). (2) 사용자가 iac-module-library에서 겪은 project-memory.json 손상 문제를 이 저장소에도 동일 적용해달라고 요청 — 조사 결과 2026-09-01 이미 pull된 permissions.deny(.claude/settings.json, notepad_*/project_memory_* MCP 도구 차단)만으로는 막히지 않는 더 깊은 원인이 있음을 이 세션에서 실측 재현: OMC SessionStart 훅(project-memory 컨텍스트 주입 로직)이 lastScanned 24시간 TTL이 지나면 MCP 도구 호출과 무관하게 project-memory.json을 자동 재스캔해 techStack/build/conventions/structure 4개 필드를 빈 자동스캔 스키마로 덮어씀(Terraform 전용 repo라 detector가 인식할 package.json류가 없어 항상 빈 결과). iac-module-library가 같은 날 오전(커밋 07c2288) 동일 문제를 lastScanned를 먼 미래 sentinel(9999999999999)로 고정하는 방식으로 해결한 것을 확인하고 이 저장소에도 동일 적용 — 손상된 4개 필드는 git 이력(HEAD)에서 복구하며 그 안의 죽은 참조(iac-platform-gitops→eks-platform-gitops 리네임 반영 등)도 함께 정정, lastScanned 고정, customNotes에 mcp-tooling-fix 노트·userDirectives에 "sentinel 값 되돌리지 말 것" 지시 추가. 다음 세션 착수 후보는 변동 없음(hub 철거 시점 미확정, 또는 dev spoke 재배포).
### 2026-09-07 00:46
2026-09-07 — 터널링 스킬(.claude/skills/argocd-tunnel-{connect,disconnect}) 상태 저장 경로를 .omc/state/argocd-tunnel → 스킬 자체 디렉토리 밑 .state/로 이전(aks-reference-infra에 이미 검증된 패턴 이식, commit aa6d4db). .gitignore에 .claude/skills/*/.state/ 추가.

이어서 .omc/project-memory.json에서 우연히 발견한 hotPaths 자동 증가(무해, MAX 50 캡)를 계기로 2026-09-01 permissions.deny 차단 자체를 재검토 → OMC 5.3.0 소스(hooks/project-memory/{index,storage,learner}.js, tools/{notepad,memory}-tools.js, lib/project-memory-merge.js) 직접 확인 → notepad_*/project_memory_read/write 도구 자체엔 재스캔·캡 로직이 없고, 실제 파괴는 SessionStart 훅과 PostToolUse 훅(도구 호출과 무관하게 항상 실행)에서 온다는 걸 확인 → permissions.deny 철회, notepad-sync SKILL.md를 도구 기반 절차로 재작성 → project_memory_add_note로 이 결정 자체를 기록해 실동작 검증(20개 캡이 정확히 발동해 가장 오래된 2개 open-items가 잘려나감, git 이력엔 그대로 남음 확인).

핵심 결정: "OMC에 완전히 의존"은 하네스 북마크(notepad/project-memory) 계층에서만 유효하고, 팀원이 알아야 할 실체 지식은 .omc/가 나중에 gitignore돼도 살아남도록 반드시 docs/*.md·CLAUDE.md에도 반영한다(상세는 project-memory customNotes의 mcp-tooling-fix 2026-09-07 항목, notepad-sync SKILL.md 참조).

다음 세션 후보: (1) 기존 notepad MANUAL·project-memory customNotes 중 팀 지식에 해당하는 항목을 docs/*.md로 승격하는 감사(.omc/ gitignore 전 필수) — 이번 세션엔 미착수. (2) hub 철거 시점 결정(변동 없음). (3) dev(spoke) 재배포(변동 없음).
### 2026-09-07 (이어서) — MANUAL 아카이브 정리
2026-09-07 (이어서) — 세션 시작 시 `notepad_read(section: "all")`이 MANUAL(1,193줄·~100KB)까지 포함해 72,520자로 토큰 한도 초과 실패한 것을 계기로 원인 진단 + 정리 수행. 원인은 파일 크기 자체가 아니라 절차 오류였다 — MANUAL은 notepad-sync 스킬 설계상 "자동 로드 안 됨"인데 세션 시작 절차가 `section: "all"`로 한 번에 불러 그 원칙을 어기고 있었음(`priority`·`working` 개별 호출은 문제없이 반환됨을 실측 확인). `notepad-sync` SKILL.md 세션 시작 절차를 `priority`+`working` 개별 호출로 수정해 재발을 막고, 후보 태스크 표시 포맷도 서술형 두 문단에서 스캔 가능한 목록 형식으로 개선(가독성 문제 사용자 지적 반영).

이어서 MANUAL 아카이브(2026-07-30~08-11 부트스트랩 Phase 1~6·workload ref→demo 전환·모듈 버전 핀 스냅샷·workbench private-only 전환 기록, 원래 1,193줄) 정리 — 사용자 승인(중복 제거 + obsolete 레거시 전체 정리) 하에 실행. 내용 검토 결과: (1) 2026-08-06 항목 블록("workbench 배선·개명·private-only 전환 완결")이 972~1074줄·1089~1204줄에 글자 그대로 두 번 들어있었다 — 파일 자신이 2026-08-11에 이미 "같은 구간이 두 벌"이라고 자기진단했지만 실제로는 지워진 적이 없었던 잔존 중복. (2) 2026-09-07 이전 세션이 fork로 이 파일 전체를 `docs/*.md`와 대조해 "승격 후보 2건·이미 문서화 3건"이라는 결론만 남기고 미완료 상태였던 감사를 이어받아 재확인 — ArgoCD 관리자 비밀번호 교체(`docs/runbooks.md` §3에 이미 완결 조건으로 명시됨)·Flow Logs confused deputy 방어(모듈 코드+`docs/hub-lifecycle.md`에 이미 반영)·D-ID 결정 식별자 전수조사(2026-08-25 코드 주석 정리 세션에서 이미 전량 제거, 현재 repo에 D-ID 패턴 0건) 등 발견한 항목 전부가 이미 최신 docs/코드에 반영돼 있음을 grep으로 실측 확인 — 추가로 승격할 미문서화 내용을 찾지 못했다(이 감사 자체가 이번 정리로 완결됨). 나머지 레거시 서술(부트스트랩 Phase 진행 로그·구 모듈 버전 핀·상태 스냅샷 등)은 이후 hub-spoke 재설계·리네임(iac-→eks-)·모듈 재핀으로 사실관계가 전부 무효화됐고, 그 시점에 남겼던 모든 "다음 태스크"는 후속 세션에서 이미 완료 처리됨(entries 자체에 ✅ 표시).

결론: MANUAL 전체(1,193줄)를 git 이력(커밋 `f4a62da` 이전)에 위임하고 이 섹션은 짧은 포인터로 교체 — 승격 누락 없음을 확인했으므로 삭제해도 지식 손실이 없다.

## MANUAL

(2026-09-07 정리) 2026-07-30~2026-08-11 사이의 상세 아카이브(부트스트랩 Phase 1~6·workload
ref→demo 전환·모듈 버전 핀 스냅샷·workbench private-only 전환 등, 원래 이 섹션 1,193줄)는
전량 검토 후 제거했다 — hub-spoke 재설계·리네임(iac-→eks-)·모듈 0.y.z 재핀으로 그 시점의
사실 대부분이 더 이상 유효하지 않았고, 남아있던 실제 결정·gotcha(ArgoCD 비밀번호 교체 절차·
Flow Logs confused deputy 방어·문서 작성 규칙 이식 등)는 이미 `docs/runbooks.md`·
`docs/hub-lifecycle.md`·`CLAUDE.md`로 승격 완료된 상태였음을 재확인했다(위 Working Memory
2026-09-07 항목 참조 — 승격 누락 없음). 원문 전체는 `git log -p .omc/notepad.md` 또는
커밋 `f4a62da`(정리 직전 HEAD) 이전 이력에서 그대로 복구 가능하다.
